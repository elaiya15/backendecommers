{
  "version": 2,
  "builds": [
    {
      "src": "register/**/*.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    { "handle": "filesystem" },
    { "src": "/.*", "dest": "/register" }
  ]
}
